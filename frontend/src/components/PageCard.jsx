function UrlComponent({pages}){
    if(!pages || pages.length === 0)
        return <p>No pages yet.</p>

    
}
export default function PageCard({pages}){
    return <>
        <UrlComponent/>
    </>
}